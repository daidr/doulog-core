debug = true

[server]
port = 3000
# 服务端地址
site = "http://localhost:3000"


[pgsql]
host = "localhost"
port = 5432
username = "username"
password = "password"
database = "doulog"

[redis]
host = "localhost"
port = 6379
auth = ""
database = 0

[auth]
# 前端 callback url prefixs
# front-end callback url prefixs, 使用场景：
#   1. OAuth2 回调校验
#   2. CORS 配置
frontend_callback_prefix = ['http://localhost:5432']
# bcrypt 加密轮数
bcrypt_rounds = 10

[auth.github]
# GitHub OAuth2 配置
# GitHub OAuth2 Config
client_id = ""
client_secret = ""

[limit.media]
# in kilobytes
file_size = 51200
#in pixels
image_size = 7680
